services:
  ferramentas:
    build: .
    expose:
      - "8501"
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - ferramentas
    restart: unless-stopped
